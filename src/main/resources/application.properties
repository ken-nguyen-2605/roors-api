server.port=${SERVER_PORT:8080}
server.forward-headers-strategy=framework

spring.application.name=roors
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5555/roors}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.defer-datasource-initialization=true

# JWT Configuration
jwt.secret=${JWT_SECRET:462a47d02e9a48590f50df088282c3ad}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Refresh Token Configuration
refresh.token.expiration=${REFRESH_TOKEN_EXPIRATION:604800000}
app.security.max-device-sessions=${APP_SECURITY_MAX_DEVICE_SESSIONS:5}

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:noreply.roors@gmail.com}
spring.mail.password=${MAIL_PASSWORD:qjdpsigvdgprcrgz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application Configuration
app.base-url=${APP_BASE_URL:http://localhost:3000}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000}
app.admin.username=${ADMIN_USERNAME:admin}
app.admin.password=${ADMIN_PASSWORD:12345678}
app.admin.email=${ADMIN_EMAIL:josephken020605@gmail.com}

# Payment Configuration (Vietnamese Bank Transfer)
payment.bank.code=${PAYMENT_BANK_CODE:970422}
payment.bank.account-number=${PAYMENT_BANK_ACCOUNT_NUMBER:0909630904}
payment.bank.account-name=${PAYMENT_BANK_ACCOUNT_NAME:NGUYEN PHUC DIEN}
payment.qr.expiry-minutes=${PAYMENT_QR_EXPIRY_MINUTES:30}

# Sepay Configuration
sepay.api-key=${SEPAY_WEBHOOK_API_KEY:your-super-secret-api-key-change-me}
sepay.webhook-secret=${SEPAY_WEBHOOK_ENABLED:true}
sepay.payment-code-prefix=ORD

# Logging
logging.level.root=${LOG_LEVEL:INFO}
logging.level.com.josephken.roors=${LOG_LEVEL:DEBUG}
logging.level.org.springframework.security=${LOG_LEVEL:INFO}
logging.level.org.springframework.web=${LOG_LEVEL:INFO}

# Log pattern with category support
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Log file configuration - ENABLED for admin viewing
logging.file.name=logs/roors-api.log
logging.file.max-size=10MB
logging.file.max-history=30
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=30